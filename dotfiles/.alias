# App replacements
alias top="htop"
alias du="ncdu"
alias df="pydf"
alias vi="vim"
# Command improvements
alias mkdir="mkdir -pv"
alias histg="history | grep"
alias lsh="ls -lah"
alias la="ls -lah"
alias ll="ls -lah"
alias tar_extract="tar xvpf"
alias tar_compress="tar cvpf"
alias tar_list="tar tvpf"
alias psgrep="ps aux | grep -v grep | grep -i -e VSZ -e"
alias ssh="ssh -Xv"
# Music
alias psychedelic="mplayer http://88.191.145.93:8010"
alias stubru="mplayer http://mp3.streampower.be/stubru-high"
# Functions
ping_date () { ping $1 | while read pong; do echo $(date +%c) — $pong;done }
mkdircd () {
    mkdir -p $1
    cd $1
}
